**标题**: 模块化工程资料生成提示（WPS 兼容）

**用途**: 指导辅助工程师或自动化代理在本仓库内，基于模块化 `.module` 包和 SQLite 规则，生成工程资料并保持向后兼容与稳定性。

**作用域**: 仓库范围（Workspace-scoped）。该提示用于：
- 解析并安装 `.module` 模块包（包含 `manifest.json`, `rules.db`, `templates/`）
- 从 `rules.db` 读取规范数据并驱动左侧模板树、模板选择与资料生成
- 使用模块化架构实现功能扩展，不破坏现有 API

---

## 输入（Inputs）
- `project_root`：工程根路径（相对路径优先），例如 `.`
- `module_name`：目标模块标识或路径（相对路径到 `.module` 文件夹）
- `task`：要执行的任务（例如：`list-templates`、`generate-doc`、`install-module`、`validate-module`）
- `options`：可选参数（模板 id、输出相对路径、日志级别等）

## 输出（Outputs）
- 结构化 JSON，至少包含 `status`, `message`, `changes[]`, `commit_id?`（如有提交）
- 在操作过程中将所有日志写入 `Logs/` 目录，返回含日志路径的参考

---

## 基本规则（必须遵守的约束）
1. 保持 WPS 兼容，不使用 Office 专属 API。所有与文档操作相关的实现应使用与 WPS 兼容的通用接口或导出中间格式。
2. 使用模块化架构，模块包后缀为 `.module`，模块内部包含 `manifest.json`、`rules.db`、`templates/`。
3. 所有规范数据从 SQLite (`rules.db`) 读取；不要把模板或规则写死在代码里。
4. 左侧模板树必须由数据库动态生成，基于 `rules.db` 的分类与索引。
5. 已创建的资料由 `ProjectDocument` 对象/模型管理，支持加载、保存与版本控制（相对路径）。
6. 所有路径使用相对路径（相对于 `project_root` 或当前项目目录）。
7. 所有日志写入 `Logs/` 目录，格式为可解析的时间戳条目；保留旧日志不被覆盖。
8. 不要破坏现有 API：任何新增接口应保持兼容层或提供适配器。
9. 所有新增功能必须保留旧功能兼容（Feature flags 或兼容分支可选）。
10. 优先保证工程资料生成的稳定性；生成流程失败时，需回滚部分变更并记录原因。
11. 每个开发阶段单独 `git commit`，在修改前先检查 `git status` 并在提示中记录该动作。
12. 模块内部的 `manifest.json` 与 `rules.db` 必须描述模板元数据、依赖关系与渲染规则，而模板文件放在 `templates/` 目录下。

---

## 推荐 `rules.db` 模式（示例）
建议在 `rules.db` 中至少包含以下表与索引，以支持模板树、元数据查询与高效渲染：

- `templates` 表（模板主表）:
  - `id` TEXT PRIMARY KEY
  - `module` TEXT -- 模块标识
  - `title` TEXT
  - `category_id` TEXT
  - `path` TEXT -- 相对路径（相对于模块的 `templates/`）
  - `metadata` JSON -- 可存放渲染默认值、占位符说明等

- `categories` 表（树结构）:
  - `id` TEXT PRIMARY KEY
  - `parent_id` TEXT NULL
  - `name` TEXT
  - `ordering` INTEGER

- `rules` 表（渲染规则/映射）:
  - `id` TEXT PRIMARY KEY
  - `template_id` TEXT
  - `rule_type` TEXT
  - `payload` JSON

- 推荐索引：`templates(module)`, `templates(category_id)`, `rules(template_id)`。

说明：这是建议模式，具体实现可扩展；但提示/工具应假定这些结构存在并优先使用参数化 SQL 查询。

## `ProjectDocument` 最小接口建议
为保证各实现可互操作并向后兼容，建议 `ProjectDocument` 对象提供以下最小方法/属性：

- `load(path: string) -> ProjectDocument`：从相对路径加载已存在资料元数据和内容。
- `save(outputPath: string) -> {status, path}`：保存当前文档到指定相对路径（自动创建目录），并返回保存结果。
- `versions() -> List[VersionMeta]`：列出该文档的版本历史（id、timestamp、author、message）。
- `checkout(versionId: string) -> ProjectDocument`：将文档恢复到指定版本（只在用户明确请求时执行）。
- `metadata`：可读写的字典/JSON，存放模板 id、输入数据摘要、生成参数等。

实现注意事项：
- 所有路径参数均为相对路径。
- `save` 应在写入前备份旧文件（保留在 `Projects/<project>/Backups/`），以便失败时回滚。
- `ProjectDocument` 的实现应尽量轻量，并提供适配器用于与旧 API 集成。

## 事务性操作与回滚策略
为保证生成流程的稳定性，推荐实现以下事务性/回滚策略：

- 操作分阶段（校验 -> 渲染 -> 写入 -> 完成），在每个阶段记录可回滚的元信息。
- 写入前先将目标文件移动到临时备份位置（`Projects/<project>/Backups/<timestamp>/`），写入成功后删除备份或保留最近 N 个备份。
- 在出现错误时，按备份顺序回滚已修改文件，并将回滚结果记录到 `Logs/`。
- 对于跨文件/跨表操作，使用显式事务（如 SQLite 的 BEGIN/COMMIT/ROLLBACK）包裹规则/元数据更新；若底层文件系统操作无法事务化，则依赖备份与补偿动作。
- 在提示响应中，保证 `changes[]` 列出所有已写入/回滚的文件和最终状态。

## 日志格式与轮转策略
建议日志条目采用可解析 JSON 或结构化文本，示例条目：

```json
{"timestamp":"2026-05-24T15:04:05Z","level":"INFO","module":"ModuleManager","action":"install-module","message":"installed module gd_building_2024","context":{"module":"gd_building_2024_1.0.0","path":"Modules/gd_building_2024_1.0.0"}}
```

注意事项：
- 所有日志写入 `Logs/` 目录，按日期或项目分子目录存放，例如 `Logs/2026-05-24/module-install.log`。
- 使用简单轮转策略（每日/大小阈值），并保留最近 N 份压缩备份。
- 错误与回滚需记录错误码与 `error.details`，便于自动化分析。

## 路径规范与跨平台注意事项
为避免路径混淆与平台差异，必须统一约定并遵守以下路径策略：

- 所有路径内部以 POSIX 风格（使用 `/`）存储与传递，外部展示或与平台交互时再做格式化。
- 接收来自用户或外部工具的路径时，应标准化（去除冗余 `./`、`../` 并解析符号链接），使用相对路径并基于 `project_root` 解析。
- 在 Windows 环境中写入磁盘前使用 API 将 POSIX 路径转为系统路径，避免硬编码反斜杠。
- 模块包内 `templates/` 中记录的 `path` 字段应保持相对且 POSIX 化，渲染时以 `module` 根路径拼接。

## 安全性与模板输入校验
为避免注入与模板滥用，必须对模板输入与规则进行严格校验：

- 所有对 `rules.db` 的查询使用参数化 SQL；不构造字符串拼接查询。
- 模板引擎必须对可插入内容进行适当转义，特别是生成 HTML、XML、或其他可执行脚本时。
- 对外部上传的模板或模块包进行签名验证或校验 manifest 中的散列（可选），并在 `Logs/` 中记录验证结果。
- 对用户可控字段设置合理的白名单与长度限制，并在异常时拒绝生成或回滚。

## 兼容性与模块版本策略
为保障向后兼容与可控演进，建议在 `manifest.json` 中明确以下字段，并在提示中遵循版本策略：

- `manifest.json` 必须包含 `id`, `version`, `compatibility`（如最低支持的主应用版本）以及 `checksum`（可选）。
- 在加载模块时，校验 `version` 并与当前系统兼容性策略比对；若不兼容，应拒绝安装并返回清晰错误信息。
- 新增功能应以 Feature Flag 或显式 API 版本化方式提供，默认保持旧行为不变。
- 在 `changes[]` 中记录 `manifest.version` 以及任何迁移步骤与兼容提示。

## Git 与自动提交策略
关于修改代码与模块安装的提交策略建议如下：

- 默认不进行自动提交。任何脚本或助手在执行会修改仓库内容前，必须先执行 `git status --porcelain` 并将结果记录到 `Logs/`。
- 若用户提供 `--auto-commit` 或明确授权，工具可对特定文件（例如安装的模块清单或配置）执行 `git add` + `git commit`，提交信息应包含 `module id/version` 与 `action`。
- 所有由该提示或脚本产生的提交，应该在 `changes[]` 中列出，并返回 `commit_id` 到操作输出 JSON 中。
- 每项独立功能更改（例如修改 `.prompt.md`、添加脚本）应作为独立提交保存。

## 指令与行为（Prompt Instructions）
当接收到请求时，请按以下顺序执行：
1. 校验并规范输入（确保 `project_root`、`module_name` 为相对路径）。
2. 检查 `git status --porcelain`：若存在未提交改动，返回错误并提示用户先提交或暂存。不要在有未提交改动时自动改动代码。记录该检查结果到 `Logs/`。
3. 若 `task` 为 `install-module`：
   - 验证 `.module` 包结构（是否包含 `manifest.json`、`rules.db`、`templates/`）。
   - 读取并校验 `rules.db`，确保主表和索引存在。
   - 将模块文件复制到 `Modules/<module_name>/`（使用相对路径），并记录到 `Logs/`。
   - 在做任何代码变更前，生成一个描述性 git 提交信息并提示进行独立提交（自动提交需得到明确授权）。
4. 若 `task` 为 `list-templates`：从 `rules.db` 查询模板树并返回用于 UI 左侧树的数据结构（JSON）。
5. 若 `task` 为 `generate-doc`：
   - 根据模板 id 从 `templates/` 加载模板文件（不把模板写死）。
   - 使用 `rules.db` 中的规则和输入数据渲染模板，生成工程资料输出到指定相对路径。
   - 每一步产生的文件和元数据由 `ProjectDocument` 管理并记录到 `Logs/`。
   - 若渲染失败，保持旧有输出不被覆盖并记录错误。
6. 所有数据库访问必须使用参数化查询以防止注入，并在操作完成后关闭连接。

---

## 示例调用（Example invocations）
- 列出模板树：

  {"project_root":".","module_name":"Modules/gd_building_2024_1.0.0","task":"list-templates"}

- 生成文档：

  {"project_root":"Projects/project-default","module_name":"Modules/gd_building_2024_1.0.0","task":"generate-doc","options":{"template_id":"GD-C3-5182","output":"Export/Generated/"}}

---

## 迭代与审阅指引
- 在每个阶段（草稿、审核、定稿）创建单独的 git 提交并记录提交信息到输出 JSON 的 `changes[]` 中。
- 将 `.prompt.md` 保存在仓库根目录以便团队共享；若需个人私有提示，建议放在 `/.vscode/` 或用户配置目录。

## 最易混淆的点（消歧义）
- “不要把模板和规则写死” 指：不要把模板内容或规则硬编码到代码；应以外部文件（`templates/`、`rules.db`）为权威。
- “保留旧功能兼容” 指：新增功能须以非侵入方式提供，不直接替换现有 API 行为，必要时增加适配层。

---

## 建议的后续自定义
- 增加示例数据集与集成测试脚本，用于验证 `generate-doc` 在不同模块下的稳定性。
- 提供一个交互式命令 `tools/install_module.py` 或 PowerShell 脚本，封装 `git status` 检查、复制模块、并生成提交信息（默认不自动提交）。

---

如果你同意，我将按此草稿创建 `.prompt.md` 文件并为该变更创建单独的 git 提交（draft）。
